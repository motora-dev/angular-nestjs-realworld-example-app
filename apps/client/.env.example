# =============================================================================
# Client Environment Variables
# =============================================================================

# Basic Authentication (for preview/develop environments)
# Set to "true" to enable basic auth protection
BASIC_AUTH_ENABLED=false

# Basic auth credentials (required if BASIC_AUTH_ENABLED=true)
# These should match the values in Secret Manager: basic-auth-user, basic-auth-password
BASIC_AUTH_USER=motora
BASIC_AUTH_PASSWORD=test

# Angular build configuration
# Options: development, preview, production
BUILD_CONFIGURATION=development
